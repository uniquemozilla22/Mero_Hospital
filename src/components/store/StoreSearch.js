import React from "react";

const StoreSearch = () => {
  return (
    <Input
      placeholder="Search"
      right
      icon="find"
      family="antdesign"
      iconSize={20}
      iconColor={colors.green}
    />
  );
};

export default StoreSearch;
